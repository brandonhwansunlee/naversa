# Naver Search Ad Optimization

Streamlit + OpenAI GPT 기반 키워드-순위 최적화 도구

## 실행
```
pip install -r requirements.txt
streamlit run app.py
```

## 배포 (Streamlit Cloud)
1. GitHub 업로드
2. Streamlit Cloud에서 repo 연결
3. Secrets 설정:
```
OPENAI_API_KEY = "YOUR_KEY"
```
